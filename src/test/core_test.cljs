(ns ebc.core-test
  (:require [clojure.test :refer :all]
            [day8.re-frame.test :as rf-test]
            [re-frame.core :as rf]
            [ebc.events :as ebce]
            [ebc.subs :as ebcs]))


